language: python
python:
  - "2.6"
  - "2.7"
virtualenv:
  system_site_packages: true
# command to install dependencies
install:
  - pip install -e .
# command to run tests
script:
  - python setup.py test